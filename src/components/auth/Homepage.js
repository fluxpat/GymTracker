// First screen that user sees. Simple option to login to go to their personalised page. I should provide a pseudo login which simply persists user's IP address. There is surely a way to do this safely.

import React from 'react';
import { Route } from 'react-router-dom';

function Homepage() {
    return (
        <div>

        </div>
    )
}

export default Homepage;